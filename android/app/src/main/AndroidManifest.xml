<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <!-- Permissions pour l'application CORAN Warsh -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <application
        android:label="CORAN Warsh"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- Activité Flutter principale -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Activités Android intégrées - Temporairement commentées pour l'APK -->
        <!--
        <activity
            android:name="com.example.mywars.PrayerPage"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.FavoritesManager"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.FavoritesActivity"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.ReadingPosition"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.ReadingManager"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.JuzzImageLoader"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.ItemSarr"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.listeSarr"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.SarrLectureActivity"
            android:exported="false" />
        <activity
            android:name="com.example.mywars.MainActivity"
            android:exported="false" />
        -->
            
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>